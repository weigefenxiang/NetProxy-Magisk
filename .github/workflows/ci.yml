name: 持续集成（构建与开发包）

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/module/**'
      - 'src/native/netproxy/**'
      - 'src/android/**'
      - 'src/webui/**'
      - 'tests/**'
      - '.github/workflows/**'
      - '.github/actions/**'
      - '.github/scripts/**'
      - '.gitattributes'

permissions:
  contents: read

# 同一分支的新推送取消尚未完成的旧构建
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: 判断受影响组件
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      actions: read
    outputs:
      module: ${{ steps.changes.outputs.module }}
      android: ${{ steps.changes.outputs.android }}
    steps:
      - name: 拉取代码
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          sparse-checkout: .github/scripts

      - name: 计算验证范围
        id: changes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node .github/scripts/ci-changes.mjs

  module:
    name: 验证并构建模块
    needs: changes
    if: needs.changes.outputs.module == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      version: ${{ steps.pack.outputs.version }}
      commit_count: ${{ steps.pack.outputs.commit_count }}
      standard_name: ${{ steps.pack.outputs.standard_name }}
      manager_name: ${{ steps.pack.outputs.manager_name }}
    steps:
      - name: 拉取代码
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          filter: blob:none

      - name: 构建并打包模块
        id: pack
        uses: ./.github/actions/build-module

      - name: 保存已验证的模块包
        uses: actions/upload-artifact@v7
        with:
          name: module-packages
          path: |
            ${{ steps.pack.outputs.standard_name }}
            ${{ steps.pack.outputs.manager_name }}
          if-no-files-found: error
          compression-level: 0
          retention-days: 3

  android:
    name: 验证 Android 管理器
    needs: changes
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: 拉取 Android 源码与构建配置
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          sparse-checkout: |
            src/android
            src/module/config/singbox
            .github/actions/verify-android

      - name: 执行 Android 验证
        uses: ./.github/actions/verify-android

      - name: 准备 Android 调试包
        shell: bash
        run: |
          set -euo pipefail
          source_apk="src/android/app/build/outputs/apk/debug/app-debug.apk"
          test -f "$source_apk"
          cp "$source_apk" NetProxy_Manager_debug.apk

      - name: 保存 Android 调试包
        uses: actions/upload-artifact@v7
        with:
          name: manager-debug-apk
          path: NetProxy_Manager_debug.apk
          if-no-files-found: error
          compression-level: 0
          retention-days: 3

  publish:
    name: 发布已验证的开发包
    needs: [module, android]
    if: >-
      always() && !cancelled() && needs.module.result == 'success' &&
      (needs.android.result == 'success' || needs.android.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      VERSION: ${{ needs.module.outputs.version }}
      COMMIT_COUNT: ${{ needs.module.outputs.commit_count }}
      STANDARD_NAME: ${{ needs.module.outputs.standard_name }}
      MANAGER_NAME: ${{ needs.module.outputs.manager_name }}
    steps:
      - name: 拉取提交记录
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          sparse-checkout: .github/scripts

      - name: 下载本次运行的模块包
        uses: actions/download-artifact@v8
        with:
          name: module-packages

      - name: 下载 Android 调试包
        if: needs.android.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: manager-debug-apk
          path: ${{ runner.temp }}/manager-debug

      - name: 设置构建元信息
        run: |
          manager_debug_name="NetProxy_Manager_${COMMIT_COUNT}_debug.apk"
          echo "TIMESTAMP=$(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S')" >> "$GITHUB_ENV"
          echo "SHORT_HASH=${GITHUB_SHA::7}" >> "$GITHUB_ENV"
          echo "COMMIT_MESSAGE=$(git log -1 --format=%s "$GITHUB_SHA")" >> "$GITHUB_ENV"
          echo "MANAGER_DEBUG_NAME=$manager_debug_name" >> "$GITHUB_ENV"
          if [ -f "$RUNNER_TEMP/manager-debug/NetProxy_Manager_debug.apk" ]; then
            cp "$RUNNER_TEMP/manager-debug/NetProxy_Manager_debug.apk" "$manager_debug_name"
          fi

      - name: 发布 Pre-release 到 GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          base_tag="$(git tag --list 'v*' --sort=-version:refname | head -n 1)"
          notes_file="$RUNNER_TEMP/nightly-notes.md"
          assets=("$STANDARD_NAME" "$MANAGER_NAME")
          if [ -f "$MANAGER_DEBUG_NAME" ]; then
            assets+=("$MANAGER_DEBUG_NAME")
          fi

          {
            echo "同步最新代码变更，仅保留最新版本的构建产物。"
            echo
            echo "版本: \`${VERSION}\` (build ${COMMIT_COUNT})"
            echo "构建时间: ${{ env.TIMESTAMP }}"
            echo "提交: \`${{ env.SHORT_HASH }}\`"
            if [ -f "$MANAGER_DEBUG_NAME" ]; then
              echo "Android 调试包: \`${MANAGER_DEBUG_NAME}\`（仅用于 nightly 真机验证）"
            fi
            echo
            echo "## 提交记录"
            echo
            if [ -n "$base_tag" ]; then
              git log --first-parent --pretty=format:"- %s ([%h](${{ github.server_url }}/${{ github.repository }}/commit/%H))" "$base_tag..$GITHUB_SHA"
              echo
              echo
              echo "完整对比: ${{ github.server_url }}/${{ github.repository }}/compare/${base_tag}...nightly"
            else
              git log --first-parent --pretty=format:"- %s ([%h](${{ github.server_url }}/${{ github.repository }}/commit/%H))" "$GITHUB_SHA"
              echo
            fi
          } > "$notes_file"

          # 删除已有的 nightly 发布和 Tag
          gh release delete nightly --cleanup-tag -y || true

          # 等待 API 状态同步
          sleep 2

          # 创建新的 nightly 发布并上传资产
          gh release create nightly \
            --target "$GITHUB_SHA" \
            --prerelease \
            --title "nightly Pre-release" \
            --notes-file "$notes_file" \
            "${assets[@]}"

      - name: Telegram 构建成功通知
        if: success()
        uses: cbrgm/telegram-github-action@v1
        with:
          token: ${{ secrets.TG_BOT_TOKEN }}
          to: ${{ secrets.TG_CHAT_ID }}
          thread-id: 2275
          parse-mode: markdown
          message: |
            **持续集成构建成功**

            版本: `${{ needs.module.outputs.version }}` (build ${{ needs.module.outputs.commit_count }})
            说明: ${{ env.COMMIT_MESSAGE }}

            [下载开发包](${{ github.server_url }}/${{ github.repository }}/releases/tag/nightly)
            [提交记录](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
            [构建日志](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})

  failure:
    name: 通知验证或发布失败
    needs: [changes, module, android, publish]
    if: always() && contains(needs.*.result, 'failure')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: 失败通知
        uses: cbrgm/telegram-github-action@v1
        with:
          token: ${{ secrets.TG_BOT_TOKEN }}
          to: ${{ secrets.TG_CHAT_ID }}
          thread-id: 2275
          parse-mode: markdown
          message: |
            **持续集成失败！**

            提交: `${{ github.sha }}`
            [点击查看构建日志](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
